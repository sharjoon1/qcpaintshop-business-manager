# Archived — Pre-Hub Pages (NAV-2)

These HTMLs are kept for bookmark/bookmarked-URL compatibility.
They 302 → canonical Hub via client-side `meta refresh` + server fallback (future) and `HUB_MAP` in `universal-nav-loader.js`.

Do NOT link these from `sidebar-v2.html`. After 2 sprints (audit `docs/audits/nav-inventory-2026-08-07.csv` + access logs), delete if zero hits.

## Canonical map

- Stock domain → `admin-stock-hub.html`
- Zoho Books → `admin-zoho-dashboard.html` (future `admin-zoho-hub.html`)
- Salary → `admin-salary-monthly.html` (future `admin-salary-hub.html`)
- CRM → `admin-leads.html` / `admin-customers.html` (future `admin-crm-hub.html`)
